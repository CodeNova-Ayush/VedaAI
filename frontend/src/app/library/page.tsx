import ComingSoon from '@/components/ComingSoon';

export default function LibraryPage(): JSX.Element {
  return (
    <ComingSoon
      title="My Library"
      description="Saved papers, rubrics, and reusable templates will appear here."
    />
  );
}
